# Copy to .env when building for production and point this at your deployed backend.
# Leave unset for local development (Vite proxy handles it instead).
VITE_API_URL=https://your-backend.onrender.com/api
